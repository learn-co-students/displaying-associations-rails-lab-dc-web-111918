<h1><%= @artist.name %>'s Songs</h1>
<h3><%= @artist.name %> has <%= @artist.song_count %>songs</h3>

<ul>
  <%= @artist.songs.each do |song| %>
    <li>
      <%= link_to song.title, song_path(song) %>
    </li>
  <% end %>
</ul>
